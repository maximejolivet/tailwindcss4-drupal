; Raised limits for Drupal (default php.ini values are too low for
; file uploads, image styles and long-running batch/cron operations).
memory_limit = 256M
upload_max_filesize = 64M
post_max_size = 64M
max_execution_time = 240
