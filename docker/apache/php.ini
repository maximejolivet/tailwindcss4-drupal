memory_limit = 256M
upload_max_filesize = 64M
post_max_size = 64M
max_execution_time = 120
max_input_vars = 5000

; Route outgoing mail to the Mailhog container instead of a real MTA.
sendmail_path = "/usr/bin/msmtp -t --host=mailhog --port=1025"
